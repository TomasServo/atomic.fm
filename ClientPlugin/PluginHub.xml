<?xml version="1.0"?>
<PluginData xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="GitHubPlugin">
  <Id>local/InternetRadio</Id>
  <GroupId>local/InternetRadio</GroupId>
  <FriendlyName>atomic.fm</FriendlyName>
  <Author>TomasServo</Author>
  <Commit>local</Commit>
  <SourceDirectories>
    <Directory>ClientPlugin</Directory>
  </SourceDirectories>
  <Tooltip>Streams atomic.fm from Icecast into the Space Engineers client.</Tooltip>
  <Description>All Ultralounge all the time.

atomic.fm adds client-side internet radio to Space Engineers through Pulsar. Mark any terminal block as a radio source by adding atomic.fm=true to Custom Data. Optional values: atomic.fm.range=35 and atomic.fm.volume=1.0.

The stream starts automatically near marked blocks with distance fade. Plain armor blocks do not work because they do not have terminal Custom Data. Audio is client-side; each player chooses whether to install and enable the plugin.</Description>
  <NuGetReferences>
    <PackageReference Include="NAudio" Version="2.2.1" />
  </NuGetReferences>
  <Hidden>false</Hidden>
</PluginData>
